service: beer-shop-products
provider:
    name: aws
    runtime: nodejs8.10
    stage: dev
    region: eu-north-1

functions:
  hello:
    handler: products/get.hello
    events:
      - http:
          path: products
          method: get
          cors: true