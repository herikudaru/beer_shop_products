language: node_js

node_js: 
  - "8"

cache:
  directories:
    - node_modules

# blocklist
branches:
  except:
  - test

  #whitelist
  only:
  - master

deploy:
  provider: script  
  script: deploy.sh
  on:
    branch: master

